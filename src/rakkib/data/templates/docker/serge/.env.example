# Serge
# Docs: https://github.com/serge-chat/serge

# Override the built-in default with a per-install generated secret.
SERGE_JWT_SECRET={{SERGE_JWT_SECRET}}

# Optional: keep default sqlite under /data/db
# SERGE_DATABASE_URL=sqlite:////data/db/sql_app.db
