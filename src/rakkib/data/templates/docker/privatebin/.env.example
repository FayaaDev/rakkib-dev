# PrivateBin (privatebin/nginx-fpm-alpine)

# Optional: override the image tag.
PRIVATEBIN_IMAGE=privatebin/nginx-fpm-alpine:stable

# Optional: timezone for php-fpm and logs.
TZ={{TZ}}
PHP_TZ={{TZ}}
